# ===================================
# Настройки подключения к PostgreSQL
# ===================================
db.url=jdbc:postgresql://localhost:5432/card_game_bd
db.username=postgres
db.password=postgres

# Схема, в которой лежат таблицы предметной области
db.schema=card_game

# Если true — приложение само создаст схему/таблицы и зальёт тестовые данные
# (idempotent: повторный запуск пересоздаст таблицы). Можно выключить, если БД
# уже инициализирована скриптами из папки db/.
db.initSchema=false

# Размер колоды по правилам игры (правило "ровно N карт")
deck.requiredSize=60

# ===================================
# Пул соединений HikariCP
# ===================================
db.pool.maximumPoolSize=10
db.pool.minimumIdle=2
db.pool.connectionTimeout=30000
db.pool.idleTimeout=600000
db.pool.maxLifetime=1800000
